# Copy to .env.local (local dev) and set the same values in Vercel project env vars.
# The deployed Modal endpoint base URL (no trailing slash):
MODAL_ENDPOINT_URL=https://s3sourabh--slm-125m-serve-model-web.modal.run
# The bearer token created with: modal secret create slm-serve-token TOKEN=...
MODAL_API_TOKEN=replace-with-your-token
